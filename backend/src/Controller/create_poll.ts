import type { ServerResponse } from "node:http";
import type { customRequest } from "../global";

export async function create_poll(req:customRequest,res:ServerResponse){
    try {
        const {}=req.body;
    } catch (error) {

    }
}
